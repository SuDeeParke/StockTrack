[project]
name = "stocktrack-backend"
version = "0.1.0"
requires-python = ">=3.11"
description = "StockTrack FastAPI backend"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
